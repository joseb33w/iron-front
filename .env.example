# Iron Front — frontend env (committed)
# Copy this to `.env` and fill with your Supabase project values.
# These are the SHARED Gogi Supabase project values for the original deploy.

VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# Required: per-user table prefix (set up when the database was provisioned).
VITE_TABLE_PREFIX=usr_xxxxxxxxxxxx
